# Formspree CLI — from the project's Settings tab at formspree.io
# Also set these as Workers Builds "Build variables and secrets",
# or as GitHub Actions secrets of the same names.
FORMSPREE_DEPLOY_KEY=
NEXT_PUBLIC_FORMSPREE_PROJECT_ID=

# Local `npm run deploy` / GitHub Actions only. Workers Builds authenticates itself.
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=
